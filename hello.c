/* 
#include <stdio.h> tells the compiler to include the Standard Input Output library. This is necessary for using printf, which outputs text.

int main(void) is the entry point of every C program. The code inside this block is executed first.

return 0; signifies that the program has finished successfully.
 */

 #include <stdio.h>

int main(void)
{
    printf("Hello, world!\n");
    return 0;
}
