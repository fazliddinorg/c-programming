/*
Arrays store multiple values of the same type.
Access using index, starting at 0.
*/

int main()
{
    int nums[3] = {10, 20, 30};
    printf("First: %d\n", nums[0]);
    nums[1] = 25;
    printf("Second: %d\n", nums[1]);
    return 0;
}
